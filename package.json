{
  "name": "london-events",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "scrape": "tsx scrape.ts",
    "debug:scrapers": "tsx index.ts",
    "site:build": "eleventy",
    "site:dev": "eleventy --serve",
    "build": "npm run scrape && npm run site:build",
    "dev": "npm run scrape && npm run site:dev"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "cheerio": "^1.0.0",
    "playwright": "^1.47.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}